name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:
  no-op:
    name: No service code to test
    runs-on: ubuntu-latest
    steps:
      - run: echo "paperclipai/ and api-gateway/ removed post-pivot. paperclip (paperclipai/paperclip) has its own CI."
